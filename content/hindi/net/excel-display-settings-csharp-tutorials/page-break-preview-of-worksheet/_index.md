---
title: वर्कशीट का पेज ब्रेक पूर्वावलोकन
linktitle: वर्कशीट का पेज ब्रेक पूर्वावलोकन
second_title: .NET API संदर्भ के लिए Aspose.Cells
description: .NET के लिए Aspose.Cells का उपयोग करके वर्कशीट का पेज ब्रेक पूर्वावलोकन दिखाने के लिए चरण दर चरण मार्गदर्शिका।
type: docs
weight: 110
url: /hi/net/excel-display-settings-csharp-tutorials/page-break-preview-of-worksheet/
---
इस ट्यूटोरियल में, हम यह बताने जा रहे हैं कि .NET के लिए Aspose.Cells का उपयोग करके वर्कशीट का पेज ब्रेक पूर्वावलोकन कैसे दिखाया जाए। वांछित परिणाम प्राप्त करने के लिए इन चरणों का पालन करें:

## चरण 1: वातावरण स्थापित करना

सुनिश्चित करें कि आपने .NET के लिए Aspose.Cells स्थापित किया है और अपना विकास वातावरण स्थापित किया है। साथ ही, सुनिश्चित करें कि आपके पास उस एक्सेल फ़ाइल की एक प्रति है जिस पर आप पेज ब्रेक पूर्वावलोकन प्रदर्शित करना चाहते हैं।

## चरण 2: आवश्यक निर्भरताएँ आयात करें

Aspose.Cells से कक्षाओं का उपयोग करने के लिए आवश्यक निर्देश जोड़ें:

```csharp
using Aspose.Cells;
using System.IO;
```

## चरण 3: कोड आरंभीकरण

अपने Excel दस्तावेज़ों वाली निर्देशिका का पथ प्रारंभ करके प्रारंभ करें:

```csharp
string dataDir = "YOUR DOCUMENTS DIRECTORY";
```

## चरण 4: एक्सेल फ़ाइल खोलना

 एक बनाने के`FileStream` खोलने के लिए एक्सेल फ़ाइल युक्त ऑब्जेक्ट:

```csharp
FileStream fstream = new FileStream(dataDir + "book1.xls", FileMode.Open);
```

 त्वरित करें ए`Workbook` ऑब्जेक्ट करें और फ़ाइल स्ट्रीम का उपयोग करके एक्सेल फ़ाइल खोलें:

```csharp
Workbook workbook = new Workbook(fstream);
```

## चरण 5: स्प्रेडशीट तक पहुँचना

एक्सेल फ़ाइल में पहली वर्कशीट पर जाएँ:

```csharp
Worksheet worksheet = workbook.Worksheets[0];
```

## चरण 6: पृष्ठ-दर-पूर्व पूर्वावलोकन प्रदर्शित करना

स्प्रैडशीट के लिए पृष्ठ-दर-पूर्व पूर्वावलोकन सक्षम करें:

```csharp
worksheet. IsPageBreakPreview = true;
```

## चरण 7: परिवर्तन सहेजना

Excel फ़ाइल में किए गए परिवर्तन सहेजें:

```csharp
workbook.Save(dataDir + "output.xls");
```

## चरण 8: फ़ाइल स्ट्रीम बंद करना

सभी संसाधनों को जारी करने के लिए फ़ाइल स्ट्रीम बंद करें:

```csharp
fstream.Close();
```

### .NET के लिए Aspose.Cells का उपयोग करके वर्कशीट के पेज ब्रेक पूर्वावलोकन के लिए नमूना स्रोत कोड 
```csharp
//दस्तावेज़ निर्देशिका का पथ.
string dataDir = "YOUR DOCUMENT DIRECTORY";
// एक फ़ाइल स्ट्रीम बनाना जिसमें एक्सेल फ़ाइल खोली जानी है
FileStream fstream = new FileStream(dataDir + "book1.xls", FileMode.Open);
// वर्कबुक ऑब्जेक्ट को इंस्टेंट करना
// फ़ाइल स्ट्रीम के माध्यम से एक्सेल फ़ाइल खोलना
Workbook workbook = new Workbook(fstream);
// एक्सेल फ़ाइल में पहली वर्कशीट तक पहुँचना
Worksheet worksheet = workbook.Worksheets[0];
// पेज ब्रेक पूर्वावलोकन में वर्कशीट प्रदर्शित करना
worksheet.IsPageBreakPreview = true;
// संशोधित एक्सेल फ़ाइल सहेजा जा रहा है
workbook.Save(dataDir + "output.xls");
// सभी संसाधनों को मुक्त करने के लिए फ़ाइल स्ट्रीम को बंद करना
fstream.Close();
```

## निष्कर्ष

इस ट्यूटोरियल में, आपने सीखा कि .NET के लिए Aspose.Cells का उपयोग करके वर्कशीट का पेज ब्रेक पूर्वावलोकन कैसे प्रदर्शित किया जाए। वर्णित चरणों का पालन करके, आप अपनी एक्सेल फ़ाइलों की उपस्थिति और लेआउट को आसानी से नियंत्रित कर सकते हैं।

### अक्सर पूछे जाने वाले प्रश्न (FAQ)

#### .NET के लिए Aspose.Cells क्या है?

.NET के लिए Aspose.Cells .NET अनुप्रयोगों में एक्सेल फ़ाइलों में हेरफेर करने के लिए एक लोकप्रिय सॉफ्टवेयर लाइब्रेरी है।

#### क्या मैं संपूर्ण वर्कशीट के बजाय किसी विशिष्ट वर्कशीट का पृष्ठ-दर-पूर्व पूर्वावलोकन दिखा सकता हूँ?

हां, Aspose.Cells का उपयोग करके आप संबंधित वर्कशीट ऑब्जेक्ट तक पहुंच कर किसी विशिष्ट वर्कशीट के लिए पेज ब्रेक पूर्वावलोकन सक्षम कर सकते हैं।

#### क्या Aspose.Cells अन्य Excel फ़ाइल संपादन सुविधाओं का समर्थन करता है?

हां, Aspose.Cells एक्सेल फ़ाइलों को संपादित करने और हेरफेर करने के लिए सुविधाओं की एक विस्तृत श्रृंखला प्रदान करता है, जैसे डेटा जोड़ना, फ़ॉर्मेट करना, चार्ट बनाना आदि।

#### क्या Aspose.Cells केवल .xls प्रारूप में Excel फ़ाइलों के साथ काम करता है?

नहीं, Aspose.Cells .xls और .xlsx सहित विभिन्न Excel फ़ाइल स्वरूपों का समर्थन करता है।
	